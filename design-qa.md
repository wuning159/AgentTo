# RAG 技术可观测工作台设计验收

验收日期：2026-07-16

## 对比材料

- 设计参考：`E:\AgentTo\artifacts\rag-workbench-source.png`
- 检索工作台实现：`E:\AgentTo\artifacts\rag-execution-workbench.png`
- 文档处理工作台实现：`E:\AgentTo\artifacts\rag-ingestion-workbench.png`
- 并排对比：`E:\AgentTo\artifacts\rag-workbench-comparison.png`

## 核对结果

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| A：全链路时间线 | 通过 | 阶段、耗时、输入输出数量和状态完整展示，点击后能切换检查器 |
| B：技术检查器 | 通过 | 支持处理说明、参数与公式、输入输出样本、原始数据四种视图 |
| RRF 公式说明 | 通过 | 展示 `1 / (k + rank)`、`k = 60` 及当前候选的两路贡献和融合分数 |
| 降级状态 | 通过 | 正常、降级、跳过、失败使用不同状态样式，摘要区域同步显示 |
| C：完整执行报告 | 通过 | 默认折叠，展开后显示事件时间、阶段、执行记录、状态和耗时 |
| 文档入库复用 | 通过 | 同一工作台可展示存储、解析、清洗、分块、向量化、索引和完成 |
| 视觉一致性 | 通过 | 沿用设计参考的绿色技术后台风格、信息层级和主次布局，并适配现有抽屉交互 |
| 浏览器控制台 | 通过 | 检索工作台和文档处理工作台均未发现前端 warning 或 error |

## 实际交互验证

1. 打开最新检索 Trace，执行工作台正常加载。
2. 点击“参数与公式”，RRF 公式、`k = 60` 和候选贡献正常显示。
3. 展开完整执行报告，表头加 7 条事件记录共 8 行。
4. 从文档详情点击“查看处理过程”，七阶段入库工作台正常显示。
5. 页面请求均正常返回，没有出现 500。

final result: passed
